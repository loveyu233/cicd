stages:
  - build
  - run

cache:
  paths:
    - target/*

job-build:
  stage: build
  script: 
    - mkdir target
    - echo "build" > target/build.txt
    - ls

job-run:
  stage: run
  script: 
    - echo "run" > target/run.txt
    - ls
    - tar -cvf target.tar target
  artifacts:
    paths:
      - target
